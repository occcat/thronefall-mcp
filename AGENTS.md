# AGENTS.md

本仓库是 Thronefall 的本地控制 / MCP 项目。所有由人或 agent 产生的 Git 提交必须遵守下面的 Conventional Commits 规范。

## Conventional Commits

完整提交说明用中文。标题行可用英文 `type` / 可选 `scope`，描述正文必须是中文。

### 格式

```
<type>(<scope>): <摘要>

<正文：改了什么>

<正文：为什么改>
```

- 标题行不超过 72 个字符。
- **强制多行**：除纯打字修正外，禁止只有标题没有正文。
- 正文只写两件事：**改动内容**和**理由**。不要写过程流水账（「先改了 A 再改了 B」）、不要写「按用户要求」「如题」、不要贴无关的聊天上下文。
- `scope` 可选；有明确模块时再写，例如 `http`、`game`、`units`、`docs`。
- 一个提交只做一件事。无关改动拆开。

### type

| type | 何时使用 |
| --- | --- |
| `feat` | 用户可感知的新能力 |
| `fix` | 修复缺陷 |
| `docs` | 只改文档 |
| `style` | 格式、空格、命名，不改行为 |
| `refactor` | 重构，不改对外行为 |
| `perf` | 性能 |
| `test` | 只加或改测试 |
| `build` | 构建脚本、依赖、工程文件 |
| `ci` | CI 配置 |
| `chore` | 仓库杂务（gitignore、许可、初始化） |
| `revert` | 回滚某次提交 |

破坏性变更在标题加 `!`，并在正文用 `BREAKING CHANGE:` 起段说明影响面和迁移方式。

```
feat(http)!: 状态接口改为分场景 schema

状态 JSON 按 Menu / Day / Night 拆成不同对象，删除旧的扁平字段。

旧客户端无法解析新结构；需要同步升级 agent。
BREAKING CHANGE: GET /state 不再返回顶层 gold 字段，改为 economy.gold。
```

### 摘要

- 祈使语气，像在下指令：「添加单位派遣接口」，不要「添加了」「已完成」。
- 只概括这一次提交，不写版本号、不写「第一版」以外的里程碑口号。
- 不要把文件列表塞进标题。

### 正文

至少两段，空行分隔：

1. **改了什么**：具体到模块、接口、文件职责，不要只写「更新代码」。
2. **为什么**：动机、约束、若不改会怎样。不要空泛写「提升可维护性」，除非紧跟具体原因。

需要时可以再加：

- 影响范围（哪些接口、存档、启动方式）
- 测试上如何验证
- 关联的限制或后续不在本次处理的问题（一行即可）

不要在提交说明里写：

- Co-authored-by / Made-with 之类的工具签名
- 与 diff 无关的计划、TODO 清单
- 对未改文件的描述

### 示例

合格：

```
feat(units): 按出生线把单位派到集结点

给 CommandUnits 增加按类型选择并写入 HomePosition 的接口，
失败时回退到 HoldPosition + SnapToNavmesh。

夜晚守线需要稳定落点；只暴露世界坐标会让调用方重复实现阵型间距。
```

```
fix(http): 场景切换时拒绝写入请求

SceneTransitionIsRunning 期间 POST 返回 409，并带上当前 sceneGeneration。

过渡期调用 Unity 对象会空引用；用世代号让过期 ID 立刻失败，而不是写到旧场景。
```

```
chore: 初始化仓库

添加 AGENTS.md 约定提交说明，添加 .gitignore 忽略 worktree 与构建产物。

后续并行分支需要统一的仓库起点和提交格式。
```

不合格：

```
update
```

```
feat: 完成插件

改了很多文件，实现了方案里的功能。
```

```
fix: 修 bug

按刚才说的改了一下。
```

### 检查

提交前确认：

1. 标题符合 `<type>(<scope>): <摘要>` 或 `<type>: <摘要>`。
2. 有空行，然后有中文正文。
3. 正文能回答「改了什么」和「为什么」。
4. diff 里没有夹带未跟踪的密钥、存档、`bin/`、`.worktree/`。
